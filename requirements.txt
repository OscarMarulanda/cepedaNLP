# Streamlit app runtime dependencies only.
# Does NOT include pipeline packages (Whisper, spaCy, PyTorch, etc.)
# which run locally on the Mac Mini and are never deployed.
#
# Use this file for Streamlit Community Cloud deployment.
# The full requirements.txt is for local development.

# Frontend
streamlit>=1.54

# LLM orchestration
anthropic>=0.39

# MCP server (in-process)
fastmcp>=3.0
pydantic>=2.5

# Database
psycopg2-binary>=2.9
pgvector>=0.2

# Embeddings (HF Inference API — no local model loaded)
huggingface-hub>=0.20
numpy>=1.26

# Visualization
plotly>=5.18
matplotlib>=3.8
pandas>=2.0

# Utilities
python-dotenv>=1.0
